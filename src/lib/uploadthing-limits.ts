/**
 * Shared image size cap for logo-style UploadThing routes.
 * Imported by the file router (server) and by client upload UI copy so limits stay aligned.
 */
export const LOGO_ROUTE_MAX_FILE_SIZE = "4MB" as const
